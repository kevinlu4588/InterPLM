# Requirements for macOS (CPU-only) installation
# Install with: pip install -r requirements_macos_fixed.txt

# Core dependencies - let pip resolve compatible versions
numpy<2  # Must be 1.x for compatibility
torch>=2.1.0,<2.2.0  # Compatible with nnsight
torchvision>=0.16.0,<0.17.0  # Matches torch 2.1.x
transformers>=4.35.0,<4.40.0  # Avoid very new versions
accelerate>=0.24.0,<0.30.0

# InterPLM specific packages
nnsight==0.2.21  # Requires torch>=2.1.0
einops==0.8.0
fair-esm==2.0.0
huggingface-hub>=0.20.0
biopython==1.84

# Data and visualization
datasets
pandas
plotly
streamlit
py3dmol
matplotlib
seaborn
scipy
scikit-learn
h5py

# Utilities
pyyaml
typed-argument-parser
umap-learn
python-dotenv
wandb
multiprocess